{
	"name": "@better-auth-kit/seed",
	"description": "Seed your Better-Auth database with deterministic, yet realistic, fake data.",
	"version": "1.0.1",
	"license": "MIT",
	"author": "ping-maxwell",
	"type": "module",
	"exports": {
		".": {
			"default": "./dist/index.js",
			"types": "./dist/index.d.ts"
		}
	},
	"dependencies": {
		"better-auth": "^1.2.4",
		"chalk": "^5.4.1",
		"log-update": "^6.1.0",
		"prompts": "^2.4.2"
	},
	"devDependencies": {
		"@types/bun": "1.2.4",
		"bun-plugin-dts": "^0.3.0"
	},
	"scripts": {
		"dev": "bun build --entrypoints ./src/index.ts --outdir ./dist --target node --format esm --sourcemap=external --minify --packages external --watch",
		"build": "bun build.ts"
	},
	"publishConfig": {
		"access": "public"
	},
	"keywords": [
		"better-auth",
		"better-auth-kit",
		"seeding",
		"plugin",
		"cli",
		"seed",
		"database"
	]
}
